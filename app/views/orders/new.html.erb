<div class="max-w-6xl mx-auto">
  <h1 class="text-3xl font-bold mb-8">주문서 작성</h1>

  <% if @order.errors.any? %>
    <div class="alert alert-error mb-6">
      <svg xmlns="http://www.w3.org/2000/svg" class="stroke-current shrink-0 h-6 w-6" fill="none" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
      </svg>
      <div>
        <p class="font-medium">주문 정보를 확인해주세요</p>
        <ul class="text-sm mt-1 space-y-1">
          <% @order.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="grid lg:grid-cols-3 gap-6">
    <!-- 주문 폼 -->
    <div class="lg:col-span-2 space-y-6">
      <%= form_with model: @order, url: orders_path, method: :post, class: "space-y-6" do |f| %>
        <!-- 배송지 정보 -->
        <div class="card bg-base-100 shadow-lg">
          <div class="card-body">
            <h2 class="card-title">배송지 정보</h2>

            <div class="form-control">
              <%= f.label :shipping_name, "받는 분", class: "label" %>
              <%= f.text_field :shipping_name,
                              value: current_user.name,
                              class: "input input-bordered",
                              required: true %>
            </div>

            <div class="form-control">
              <%= f.label :shipping_phone, "연락처", class: "label" %>
              <%= f.telephone_field :shipping_phone,
                                    value: current_user.phone,
                                    class: "input input-bordered",
                                    placeholder: "010-1234-5678",
                                    required: true %>
            </div>

            <div class="grid grid-cols-3 gap-4">
              <div class="form-control col-span-1">
                <%= f.label :shipping_zip_code, "우편번호", class: "label" %>
                <%= f.text_field :shipping_zip_code,
                                class: "input input-bordered",
                                placeholder: "12345" %>
              </div>
            </div>

            <div class="form-control">
              <%= f.label :shipping_address, "주소", class: "label" %>
              <%= f.text_area :shipping_address,
                              class: "textarea textarea-bordered h-24",
                              placeholder: "상세 주소를 입력해주세요",
                              required: true %>
            </div>

            <div class="form-control">
              <%= f.label :delivery_memo, "배송 메모", class: "label" %>
              <%= f.text_area :delivery_memo,
                              class: "textarea textarea-bordered",
                              placeholder: "배송 시 요청사항이 있으시면 입력해주세요 (선택사항)",
                              rows: 3 %>
            </div>
          </div>
        </div>

        <!-- 주문 상품 확인 -->
        <div class="card bg-base-100 shadow-lg">
          <div class="card-body">
            <h2 class="card-title mb-4">주문 상품</h2>

            <div class="space-y-6">
              <% @cart_items_by_farmer.each do |farmer, items| %>
                <div class="border border-base-300 rounded-lg p-4">
                  <div class="flex items-center gap-3 mb-4">
                    <div class="avatar placeholder">
                      <div class="bg-primary text-primary-content rounded-full w-10">
                        <span><%= farmer.name[0] %></span>
                      </div>
                    </div>
                    <div>
                      <h3 class="font-bold"><%= farmer.name %></h3>
                      <p class="text-sm text-base-content/70"><%= farmer.location %></p>
                    </div>
                  </div>

                  <div class="space-y-2">
                    <% items.each do |cart_item| %>
                      <div class="flex justify-between items-center py-2 border-b border-base-200 last:border-0">
                        <div>
                          <p class="font-medium"><%= cart_item.product.name %></p>
                          <p class="text-sm text-base-content/70">
                            <%= number_to_currency(cart_item.product.price) %>원 × <%= cart_item.quantity %>개
                          </p>
                        </div>
                        <p class="font-bold text-primary">
                          <%= number_to_currency(cart_item.subtotal) %>원
                        </p>
                      </div>
                    <% end %>
                  </div>

                  <div class="flex justify-between items-center mt-4 pt-4 border-t border-base-300">
                    <span class="font-medium">농가별 소계</span>
                    <span class="text-lg font-bold text-accent">
                      <%= number_to_currency(items.sum(&:subtotal)) %>원
                    </span>
                  </div>
                </div>
              <% end %>
            </div>
          </div>
        </div>

        <div class="card-actions">
          <%= f.submit "주문하기", class: "btn btn-primary btn-lg btn-block" %>
          <%= link_to "취소", cart_path, class: "btn btn-ghost btn-lg btn-block" %>
        </div>
      <% end %>
    </div>

    <!-- 주문 요약 -->
    <div class="lg:col-span-1">
      <div class="card bg-base-100 shadow-lg sticky top-4">
        <div class="card-body">
          <h3 class="card-title">주문 요약</h3>

          <div class="space-y-3 py-4">
            <div class="flex justify-between">
              <span>총 농가 수</span>
              <span class="font-medium"><%= @cart_items_by_farmer.count %>곳</span>
            </div>

            <div class="flex justify-between">
              <span>총 상품 수</span>
              <span class="font-medium"><%= @cart_items.count %>개</span>
            </div>

            <div class="flex justify-between">
              <span>총 수량</span>
              <span class="font-medium"><%= @cart_items.sum(&:quantity) %>개</span>
            </div>

            <div class="divider"></div>

            <div class="flex justify-between items-center text-lg">
              <span class="font-bold">총 결제 금액</span>
              <span class="text-2xl font-bold text-primary">
                <%= number_to_currency(@cart_items.sum(&:subtotal)) %>원
              </span>
            </div>
          </div>

          <div class="alert alert-info text-sm">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" class="stroke-current shrink-0 w-5 h-5">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
            <div>
              <p>농가별로 개별 주문이 생성됩니다.</p>
              <p class="text-xs mt-1">각 농가의 승인 후 결제 진행</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
